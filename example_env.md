# Example Environment Variables

```env
# Make sure to replace username, password, endpoint, and database name
DATABASE_URL=postgres://youruser:yourpass@your.endpoint/dbname

# Private keys are SHA-256 and can be generated with:
# openssl rand -hex 32
PRIVATE_KEY=06962f20dfd573613b192fc4a85d44d3e253640a64f79d3da1dda8835269b0e4
# The private key would look something like this
